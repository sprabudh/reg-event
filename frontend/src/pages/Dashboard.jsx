const Dashboard = () => {
    return (
        <div>
            <h2>Dashboard</h2>
            <p>Welcome to the Event Registration System. Use the navigation bar to manage events.</p>
        </div>
    );
};

export default Dashboard;