const EventsList = () => {
    return (
        <div>
            <h2>Events List</h2>
            <p>We will display the events from the database here shortly!</p>
        </div>
    );
};

export default EventsList;